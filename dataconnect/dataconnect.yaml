specVersion: "v1"
serviceId: "my-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
